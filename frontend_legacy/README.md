# Starry Class Frontend

## Setup

1. Edit `config.js` and set `API_BASE_URL` to your backend HTTP address.
2. Deploy this folder to GitHub Pages.

## Notes

- Register requires one-time invite code (admin generated).
- If the page is served over HTTPS while API is HTTP, the browser may block requests.
- Use an HTTP entry if needed or enable HTTPS on the backend.
